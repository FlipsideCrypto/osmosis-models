version: 2
models:
  - name: core__dim_liquidity_pools
    description: "Deprecating soon: This is a notice that we're replacing this table with defi.dim_liquidity_pools. Please migrate queries to the new table by 9/14/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: BLOCKCHAIN
        description: "{{ doc('blockchain') }}"
        tests: 
          - dbt_expectations.expect_column_to_exist
      - name: POOL_CREATED_BLOCK_ID
        description: "{{ doc('block_id') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: POOL_CREATED_BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: MODULE
        description: "{{ doc('pool_module') }}"
        tests: 
          - dbt_expectations.expect_column_to_exist
      - name: POOL_ID
        description: "{{ doc('pool_id') }}"
        tests: 
          - dbt_expectations.expect_column_to_exist
      - name: POOL_ADDRESS
        description: "{{ doc('pool_address') }}"
      - name: ASSETS
        description: "{{ doc('pool_assets') }}"
        tests: 
          - dbt_expectations.expect_column_to_exist